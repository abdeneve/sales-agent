# Agente de Atendimento e Vendas

Backend do agente de atendimento inbound com LangGraph + FastAPI.

## Setup

```bash
uv sync --dev
uv run uvicorn app.main:app --reload
```

## Testes

```bash
uv run pytest
```
